{{- with .Values.crds -}}
{{- $app_name := (printf "%s-%s" "crds" $.Values.global.env.cloud_short_name) }}
{{- if .enabled -}}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $app_name }}
  namespace: argocd
  {{- with .finalizers }}
  finalizers:
  {{- toYaml . | nindent 6 }}
  {{- end }}
  labels:
  {{- with $.Values.labels }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .labels }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  destination:
    server: {{ $.Values.global.env.kubernetes.server }}
    {{- if .namespace }}
    namespace: {{ .namespace }}
    {{- end }}
  project: {{ .project | default "default" }}
  sources:
  {{- range $operator_name, $operator_obj := .operators }}
  {{- if $operator_obj.enabled }}
  - path: {{ tpl (printf "%s/%s" $.Values.repository.paths.crds $operator_name) $ }}
    {{- with $operator_obj.directory }}
    directory:
    {{- toYaml . | nindent 6 }}
    {{- end }}
    repoURL: {{ include "infra.repoURL" $ }}
    targetRevision: {{ $operator_obj.revision | default $.Values.crds.revision | default (required "repository.revision is required" (tpl $.Values.repository.revision $)) }}
  {{- end }}
  {{- end }}
  {{- with .syncPolicy }}
  syncPolicy:
  {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
