{{- range $name, $secret := .Values.secrets }}
{{- $labels := include "thanos.labels" $ | fromYaml }}
{{- $secret = set $secret "name" ( printf "%s-%s" (include "thanos.fullname" $ )  $name ) }}
{{- include "core.secret" (list $ $labels $secret ) }}
{{- end }}
