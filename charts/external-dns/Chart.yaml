apiVersion: v2
name: external-dns
version: 0.1.0
dependencies:
  - name: external-dns
    version: 0.1.0
    repository: file://../chart_deps/external-dns
    alias: external-dns
    condition: external-dns.enabled
