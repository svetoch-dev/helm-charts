{{- range $name, $secret := .Values.secrets }}
{{- $labels := include "prometheus-operated.labels" $ | fromYaml }}
{{- $secret = set $secret "name" ( printf "%s-%s" (include "prometheus-operated.fullname" $ )  $name ) }}
{{- include "core.secret" (list $ $labels $secret ) }}
{{- end }}
