{{- range $name, $clMultilineParsers :=  .Values.clusterMultilineParsers }}
{{- $labels := include "fluentbit-operated.labels" $ | fromYaml }}
{{- $clMultilineParsers = set $clMultilineParsers "name" ( printf "%s-parsers-%s" (include "fluentbit-operated.fullname" $ )  $name ) }}
{{- include "fluentbit.clustermultilineparsers" (list $ (toYaml $labels) $clMultilineParsers ) }}
{{- end }}
