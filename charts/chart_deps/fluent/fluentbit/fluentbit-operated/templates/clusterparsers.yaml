{{- range $name, $clParser :=  .Values.clusterParsers }}
{{- $labels := include "fluentbit-operated.labels" $ | fromYaml }}
{{- $clParser = set $clParser "name" ( printf "%s-parsers-%s" (include "fluentbit-operated.fullname" $ )  $name ) }}
{{- include "fluentbit.clusterparser" (list $ (toYaml $labels) $clParser ) }}
{{- end }}
