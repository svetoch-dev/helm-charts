{{- range $name, $secret := .Values.secrets }}
{{- $labels := include "fluentbit.labels" $ | fromYaml }}
{{- $secret = set $secret "name" ( printf "%s-%s" (include "fluentbit.fullname" $ )  $name ) }}
{{- include "core.secret" (list $ $labels $secret ) }}
{{- end }}
